__pycache__/
*.pyc
.DS_Store
fal.key
work_*.log
nohup.out

# Huge intermediate re-encodes and scratch — regenerated by the pipeline
work/norm/
work/review/
work/manifest.json
work/audio/song_*.mp3
work/audio/instrumental_*.mp3

# 1080p master exceeds GitHub's 100MB file limit — regenerate it locally (see RENDER.md).
# The raw stills/clips/audio and the 720p ARE committed so assembly is free to re-run.
output/final.mp4
